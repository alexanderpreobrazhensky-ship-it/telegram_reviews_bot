FROM python:3.11-slim

WORKDIR /app

# Bothost ищет эти пакеты - установим их
RUN pip install python-dotenv==1.0.0

# Копируем requirements.txt
COPY requirements.txt .
RUN pip install -r requirements.txt

# Копируем код
COPY . .

CMD ["python", "main.py"]
