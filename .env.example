# Client bot configuration
CLIENT_TELEGRAM_BOT_TOKEN=
CLIENT_BOT_MODE=polling
CLIENT_WEBAPP_ENABLED=1
WEBAPP_PATH=/WEBAPP
PUBLIC_BASE_URL=https://<railway-domain>
CLIENT_WEBAPP_URL=https://<railway-domain>/WEBAPP

# WebApp must be opened from the bot button, otherwise initData is empty.
